# =============================================================================
# TechAssassin Frontend - Environment Variables
# =============================================================================
# 
# IMPORTANT: 
# - Copy this file to .env.local for local development
# - Copy this file to .env.production for production builds
# - Never commit .env.local or .env.production to version control
# - Vite exposes variables prefixed with VITE_ to the client
#
# =============================================================================

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# Base URL for the TechAssassin backend API
#
# DEVELOPMENT: http://localhost:3000/api
# PRODUCTION: https://your-backend-domain.vercel.app/api
# 
# Note: Include /api at the end as all backend routes are under /api
VITE_API_URL=http://localhost:3001/api

# -----------------------------------------------------------------------------
# Supabase Configuration (Frontend)
# -----------------------------------------------------------------------------
# These are the same values as in the backend .env.local
# Get these from: Supabase Dashboard > Settings > API
#
# IMPORTANT: Only use the anon/public key here (safe for client-side)
# NEVER use the service role key in the frontend!
#
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Application name (used in titles, meta tags, etc.)
VITE_APP_NAME=TechAssassin

# Application URL (used for sharing, redirects, etc.)
# DEVELOPMENT: http://localhost:8080
# PRODUCTION: https://yourdomain.com
VITE_APP_URL=http://localhost:8080

# Enable debug mode (shows additional logging in console)
# Set to 'true' for development, 'false' for production
VITE_DEBUG=true

# -----------------------------------------------------------------------------
# Optional: Feature Flags
# -----------------------------------------------------------------------------
# Enable/disable features for testing or gradual rollout
# VITE_FEATURE_REALTIME=true
# VITE_FEATURE_NOTIFICATIONS=true
# VITE_FEATURE_ANALYTICS=false

# -----------------------------------------------------------------------------
# Development Notes
# -----------------------------------------------------------------------------
# 
# Local Development Setup:
# 1. Copy this file to .env.local
# 2. Update VITE_API_URL to point to your local backend (http://localhost:3000/api)
# 3. Update Supabase credentials to match your backend/.env.local
# 4. Run: npm run dev
#
# Production Setup:
# 1. Copy this file to .env.production
# 2. Update VITE_API_URL to your production backend URL
# 3. Update Supabase credentials to production values
# 4. Update VITE_APP_URL to your production domain
# 5. Set VITE_DEBUG=false
# 6. Run: npm run build
#
# -----------------------------------------------------------------------------
