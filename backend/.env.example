# =============================================================================
# TechAssassin Backend - Environment Variables
# =============================================================================
# 
# IMPORTANT: 
# - Copy this file to .env.local for local development
# - Never commit .env.local to version control
# - Use separate values for development, staging, and production
# - Keep service role keys and API keys secret
#
# =============================================================================

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
# Get these values from: Supabase Dashboard > Settings > API
#
# PRODUCTION: Use your production Supabase project
# DEVELOPMENT: Use your development Supabase project
# 
# Project URL format: https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url

# Anon/Public Key - Safe to expose in browser (used for client-side operations)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service Role Key - KEEP SECRET! Server-side only (admin operations)
# WARNING: This key bypasses Row Level Security - never expose to client
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# -----------------------------------------------------------------------------
# Resend Configuration (Email Service)
# -----------------------------------------------------------------------------
# Get your API key from: Resend Dashboard > API Keys
#
# PRODUCTION: Use production API key with verified domain
# DEVELOPMENT: Use test API key (sends to verified email only)
#
# Format: re_xxxxxxxxxxxxx
RESEND_API_KEY=your_resend_api_key

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------
# Base URL of your application (used for email links, redirects, etc.)
#
# PRODUCTION: https://yourdomain.com
# STAGING: https://staging.yourdomain.com or Vercel preview URL
# DEVELOPMENT: http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Optional: Additional Configuration
# -----------------------------------------------------------------------------

# Node Environment (automatically set by Vercel)
# NODE_ENV=production

# Enable debug logging (development only)
# DEBUG=true

# Custom CORS origins (comma-separated)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Rate limiting configuration
# RATE_LIMIT_WINDOW_MS=3600000
# RATE_LIMIT_MAX_REQUESTS=5

# -----------------------------------------------------------------------------
# Production Deployment Checklist
# -----------------------------------------------------------------------------
# Before deploying to production, ensure:
# 
# ✅ Separate Supabase project for production
# ✅ All database migrations applied
# ✅ Row Level Security policies enabled
# ✅ Storage buckets created with proper policies
# ✅ Realtime enabled on required tables
# ✅ Resend domain verified
# ✅ OAuth providers configured (if using)
# ✅ Environment variables set in Vercel
# ✅ CORS configured for your frontend domain
# ✅ Backups enabled in Supabase
# ✅ Monitoring and alerts configured
#
# See DEPLOYMENT.md for detailed deployment instructions
# -----------------------------------------------------------------------------
